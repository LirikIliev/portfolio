import classes from './Movies.module.scss';

const Movies: React.FC = () => {
  return (
    <div className={classes.Movies_wrapper}>
      <p>here will be all list of movies, plus filter into the head</p>
    </div>
  );
};
export default Movies;
